import 'package:flutter/material.dart';

class IconConstants {
  static const Icon darkModeIcon = Icon(Icons.dark_mode_rounded, color: Colors.black);
  static const Icon deleteIcon = Icon(Icons.delete, color: Colors.white);
  static const Icon imageErrorIcon = Icon(Icons.image_not_supported, size: 40, color: Color(0xFFBDBDBD));
  static const Icon addIcon = Icon(Icons.add, color: Colors.white);
}
